{
  "name": "site",
  "version": "0.0.0",
  "description": "An 11ty website built from JSON data exported from nonplain markdown notes",
  "main": "index.js",
  "scripts": {
    "build": "npm run notes && npx @11ty/eleventy",
    "dev": "npm run notes && npx @11ty/eleventy --serve",
    "notes": "node build-notes.js"
  },
  "license": "MIT",
  "devDependencies": {
    "nonplain": "^0.0.5",
    "nonplain-md-link": "^0.0.9",
    "slug": "^4.0.3"
  }
}
